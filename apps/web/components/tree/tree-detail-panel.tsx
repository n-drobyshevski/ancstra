'use client';

import type { PersonListItem, TreeData } from '@ancstra/shared';

export function TreeDetailPanel(props: {
  person: PersonListItem;
  treeData: TreeData;
  onClose: () => void;
  onFocusNode: (id: string) => void;
}) {
  return null;
}
